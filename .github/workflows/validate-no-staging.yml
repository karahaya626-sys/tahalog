name: Validate no staging files

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    if: github.head_ref == 'fix/factomancer-original-webp'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test ! -d .asset-staging
